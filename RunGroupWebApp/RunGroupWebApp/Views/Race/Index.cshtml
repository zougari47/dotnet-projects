@model IEnumerable<Race>

<h1 class="text-success text-center mb-5">Hello from race view</h1>

<ul class="row row-cols-3 list-unstyled">
    @foreach (var item in Model)
    {
        <li class="mb-3">
            <div class="card">
                <img src="@item.Image" class="card-img-top" alt="...">
                <div class="card-body">
                    <h5 class="card-title">@item.Title</h5>
                    <h6 class="card-subtitle mb-2 text-muted">@item.RaceCategory</h6>
                    <p class="card-text">@item.Description</p>
                    <a class="btn btn-primary">Edit</a>
                    <a class="btn btn-secondary" asp-controller="Race" asp-action="Detail" asp-route-id="@item.Id">View</a>
                </div>
            </div>
        </li>
    }
</ul>